import sys, os"""Module to allow easy inclusion of folders in the package search path.Just import the module to add the folder paths.To add new folders, simply add the paths to the newPaths array"""newPaths = [		'tools'		]def initPackagePaths():	#Add the new folders to the package path	for folder in newPaths:		thePath = os.path.join(os.getcwd(), folder)		if thePath not in sys.path:			sys.path.append(thePath)#Auto initialize the pathsinitPackagePaths()